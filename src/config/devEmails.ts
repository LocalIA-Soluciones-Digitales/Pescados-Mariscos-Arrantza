// Mantener en sync con la lista de emails de las políticas RLS
// en supabase/schema.sql (error_logs, visits, settings).
export const DEVELOPER_EMAILS = ['edortadossantos@gmail.com', 'admin@developers.local'];
