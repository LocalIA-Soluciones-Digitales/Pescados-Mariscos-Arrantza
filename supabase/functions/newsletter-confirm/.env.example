# Solo para desarrollo local: supabase functions serve newsletter-confirm --env-file supabase/functions/newsletter-confirm/.env.local
# En producción estos valores se configuran con `supabase secrets set NOMBRE=valor`,
# nunca se escriben en el código ni se suben al repositorio.

RESEND_API_KEY=
NEWSLETTER_EMAIL_FROM=
NEWSLETTER_CONFIRM_SECRET=
SITE_URL=https://arrantza.es
